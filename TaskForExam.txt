Вариант 0

Dockerfile
В качестве исходного образа взять                        	ubuntu					
Разместить информацию об авторе                          	как метку							2
Установить модуль 				         	gcc					
Скопировать файл исходного кода в контейнер в заданную папку 	создать папку MyProj и скопировать туда	
Скомпилировать его в файл с именем				myProgram			
запустить c параметром, заданным               			при старте docker

Написать метод вычисления площади круга


Вариант 1

Dockerfile

В качестве исходного образа взять                        	ubuntu					
разместить информацию об авторе                          	как метку				
Установить модуль 				         	gcc							
Скопировать файл исходного кода в контейнер в заданную папку 	создать папку MyProj и скопировать туда	
, скомпилировать его в файл с именем				myProgram				
и запустить c параметром, заданным               		при построении образа   

Написать метод вычисления площади квадрата (квадрат может быть повернут)


Вариант 2
Dockerfile

Dockerfile
В качестве исходного образа взять                        	ubuntu					
Разместить информацию об авторе                          	как метку							2
Установить модуль 				         	gcc					
Скопировать файл исходного кода в контейнер в заданную папку 	создать папку MyProj и скопировать туда	
Скомпилировать его в файл с именем				main.exe			
запустить c параметром, заданным               			при старте docker

Написать метод вычисления площади треугольника



Вариант 3

Dockerfile

В качестве исходного образа взять                        	ubuntu					
разместить информацию об авторе                          	как метку				
Установить модуль 				         	gcc							
Скопировать файл исходного кода в контейнер в заданную папку 	создать папку MyProj и скопировать туда	
, скомпилировать его в файл с именем				main.exe				
и запустить c параметром, заданным               		при построении образа 

Написать метод вычисления периметра круга

Вариант 4

Dockerfile
В качестве исходного образа взять                        	ubuntu					
Разместить информацию об авторе                          	как метку							2
Установить модуль 				         	gcc					
Скопировать файл исходного кода в контейнер в заданную папку 	в корень
Скомпилировать его в файл с именем				myProgram			
запустить c параметром, заданным               			при старте docker

Написать метод вычисления периметра квадрата (он может быть повернут)


Вариант 5

Dockerfile

В качестве исходного образа взять                        	ubuntu					
разместить информацию об авторе                          	как метку				
Установить модуль 				         	gcc							
Скопировать файл исходного кода в контейнер в заданную папку 	в корень
, скомпилировать его в файл с именем				myProgram				
и запустить c параметром, заданным               		при построении образа   

Написать метод вычисления периметра треугольника


Вариант 6

Dockerfile
В качестве исходного образа взять                        	ubuntu					
Разместить информацию об авторе                          	как метку							2
Установить модуль 				         	gcc					
Скопировать файл исходного кода в контейнер в заданную папку 	в корень и скопировать туда	
Скомпилировать его в файл с именем				main.exe			
запустить c параметром, заданным               			при старте docker

Проверить, что заданная фигура квадрат


Вариант 7

Dockerfile

В качестве исходного образа взять                        	ubuntu					
разместить информацию об авторе                          	как метку				
Установить модуль 				         	gcc							
Скопировать файл исходного кода в контейнер в заданную папку 	в корень и скопировать туда	
, скомпилировать его в файл с именем				main.exe				
и запустить c параметром, заданным               		при построении образа 

Проверить, что заданная фигура квадрат

Вариант 8

Dockerfile
В качестве исходного образа взять                        	ubuntu					
Разместить информацию об авторе                          	вывести на экран							2
Установить модуль 				         	gcc					
Скопировать файл исходного кода в контейнер в заданную папку 	создать папку MyProj и скопировать туда	
Скомпилировать его в файл с именем				myProgram			
запустить c параметром, заданным               			при старте docker

Написать метод вычисления площади круга


Вариант 9

Dockerfile

В качестве исходного образа взять                        	ubuntu					
разместить информацию об авторе                          	вывести на экран				
Установить модуль 				         	gcc							
Скопировать файл исходного кода в контейнер в заданную папку 	создать папку MyProj и скопировать туда	
, скомпилировать его в файл с именем				myProgram				
и запустить c параметром, заданным               		при построении образа   

Написать метод вычисления периметра треугольника


Вариант 10
Dockerfile

Dockerfile
В качестве исходного образа взять                        	ubuntu					
Разместить информацию об авторе                          	вывести на экран							2
Установить модуль 				         	gcc					
Скопировать файл исходного кода в контейнер в заданную папку 	создать папку MyProj и скопировать туда	
Скомпилировать его в файл с именем				main.exe			
запустить c параметром, заданным               			при старте docker

Написать метод вычисления периметра квадрата (он может быть повернут)


Вариант 11

Dockerfile
Dockerfile

В качестве исходного образа взять                        	ubuntu					
разместить информацию об авторе                          	вывести на экран				
Установить модуль 				         	gcc							
Скопировать файл исходного кода в контейнер в заданную папку 	создать папку MyProj и скопировать туда	
, скомпилировать его в файл с именем				main.exe				
и запустить c параметром, заданным               		при построении образа 

Написать метод вычисления периметра круга


Вариант 12

Dockerfile
В качестве исходного образа взять                        	ubuntu					
Разместить информацию об авторе                          	вывести на экран							2
Установить модуль 				         	gcc					
Скопировать файл исходного кода в контейнер в заданную папку 	в корень
Скомпилировать его в файл с именем				myProgram			
запустить c параметром, заданным               			при старте docker

Написать метод вычисления площадь круга


Вариант 13

Dockerfile

В качестве исходного образа взять                        	ubuntu					
разместить информацию об авторе                          	вывести на экран				
Установить модуль 				         	gcc							
Скопировать файл исходного кода в контейнер в заданную папку 	в корень
, скомпилировать его в файл с именем				myProgram				
и запустить c параметром, заданным               		при построении образа   

Написать метод вычисления площади треугольника


Вариант 14

Dockerfile
В качестве исходного образа взять                        	ubuntu					
Разместить информацию об авторе                          	вывести на экран							2
Установить модуль 				         	gcc					
Скопировать файл исходного кода в контейнер в заданную папку 	в корень и скопировать туда	
Скомпилировать его в файл с именем				main.exe			
запустить c параметром, заданным               			при старте docker

Написать метод вычисления периметра треугольника


Вариант 15

Dockerfile

В качестве исходного образа взять                        	ubuntu					
разместить информацию об авторе                          	вывести на экран				
Установить модуль 				         	gcc							
Скопировать файл исходного кода в контейнер в заданную папку 	в корень и скопировать туда	
, скомпилировать его в файл с именем				main.exe				
и запустить c параметром, заданным               		при построении образа 

Написать метод вычисления периметра треугольника
